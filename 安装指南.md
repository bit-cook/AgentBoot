# AgentBoot 安装指南

> **版本 v1.0.0** · 支持 Linux / macOS / Windows · 界面默认中文
> AgentBoot 是一个极简、极速、开箱即用的 AI Agent 启动器：内置一个保底最小 Agent（默认 Agnes 免费模型），其余 Agent（Claude Code、Codex、Qwen Code、OpenCode、CodeBuddy、MiMo Code、Cline、Hermes Agent、OpenClaw、Gemini CLI、iFlow CLI、Aider）通过**菜单自选安装**。

---

## ⚡ 一键在线安装（推荐，机器需联网）

**Linux / macOS：**

```bash
curl -fsSL https://boot.ide.pub/install.sh | sh
```

**Windows（PowerShell / CMD 二选一）：**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -Command "iex ((New-Object Net.WebClient).DownloadString('https://boot.ide.pub/install.ps1'))"
```

```bat
:: 或下载 install.bat 双击运行（GitHub 仓库根目录有该文件）
```

**镜像兜底**（当 `boot.ide.pub` 不可达时，安装脚本内部会按顺序自动尝试以下下载源，无需手动操作；也可手动改用下面的入口脚本）：

```bash
# GitHub Pages 入口
curl -fsSL https://bit-cook.github.io/AgentBoot/install.sh | sh
# 直连 GitHub
curl -fsSL https://raw.githubusercontent.com/bit-cook/AgentBoot/main/install.sh | sh
# 国内加速代理
curl -fsSL https://ghfast.top/https://raw.githubusercontent.com/bit-cook/AgentBoot/main/install.sh | sh
```

**安装完成后：**

| 命令 | 作用 |
|---|---|
| `agentboot` | 打开中文控制台菜单（安装其他 Agent / 模型配置 / 镜像代理） |
| `ab` | 直接启动内置最小 Agent（默认 Agnes 免费模型，开箱即用） |

> Windows 新装命令需**重新打开终端**才会进入 PATH。

---

## 📦 一键离线安装（目标机器无网络 / 网络受限）

离线安装只需三步：**① 拿到离线包 → ② 拷贝到目标机解压 → ③ 运行离线安装脚本**。
全程不需要目标机器联网，也不需要安装任何解压软件（解压方案见下一节）。

### 第 1 步：获取离线安装包

- **方式 A（推荐）**：到 GitHub Releases 下载现成的离线包
  `https://github.com/bit-cook/AgentBoot/releases` → 选择
  - `AgentBoot-offline-v1.0.0.zip`（Windows 机器用）
  - `AgentBoot-offline-v1.0.0.tar.gz`（Linux / macOS 用）
  - `AgentBoot-offline-v1.0.0-sfx.sh`（POSIX 自解压单文件，目标机连 tar 都不用碰）
- **方式 B（自建）**：在有网的 Linux/macOS 或 Windows 机器上运行项目自带的构建脚本，
  自动从 npmmirror 镜像拉取全部 Agent 离线载荷并打包：

  ```bash
  # Linux / macOS（可自定义平台与 Agent 列表）
  sh scripts/build-offline.sh
  AGENTS="claude-code,codex" PLATFORMS="linux-x64,win-x64" sh scripts/build-offline.sh

  # Windows（PowerShell）
  powershell -File scripts\build-offline.ps1
  ```

### 第 2 步：解压离线包（目标机器没有解压软件也能做）

离线包提供 **4 种解压路径**，按目标机器情况任选其一：

| 目标机器情况 | 解压方法 |
|---|---|
| **Windows（任意版本）** | 直接**右键 ZIP → 全部解压缩**。Windows 资源管理器自带 ZIP 支持，无需安装任何软件 |
| **Windows 10/11（命令行党）** | 系统自带 `tar`：`tar -xf AgentBoot-offline-v1.0.0.zip` |
| **Linux / macOS** | 系统自带 `tar`：`tar -xzf AgentBoot-offline-v1.0.0.tar.gz -C ~` |
| **Linux/macOS 极简环境（无 tar 权限顾虑）** | 用自解压单文件：`sh AgentBoot-offline-v1.0.0-sfx.sh`，脚本自动完成解压+安装（内部只用 base64 + tar，均为系统自带） |

> 自解压包 `*-sfx.sh` 本身就是一个 shell 脚本 + 内嵌 base64 数据，`sh xxx-sfx.sh` 即自动解压并直接进入安装流程，非常适合只有最小化系统的服务器。

### 第 3 步：运行离线安装脚本

**Linux / macOS：**

```bash
cd ~/AgentBoot
sh install-offline.sh                    # 安装程序本体 + 打开控制台菜单（推荐）
sh install-offline.sh --all              # 一键安装全部支持离线的 Agent（不进菜单）
sh install-offline.sh claude-code qwen-code   # 只安装指定 Agent
```

**Windows：**

```powershell
cd AgentBoot
powershell -NoProfile -ExecutionPolicy Bypass -File install-offline.ps1          # 安装 + 打开菜单
powershell -NoProfile -ExecutionPolicy Bypass -File install-offline.ps1 -All     # 全部 Agent
powershell -NoProfile -ExecutionPolicy Bypass -File install-offline.ps1 -Agents claude-code,qwen-code
```

> 也可以右键 `install-offline.ps1` →「使用 PowerShell 运行」。
> 若提示执行策略限制，用上面带 `-ExecutionPolicy Bypass` 的命令即可。

### 离线安装脚本做了什么（透明化）

1. 校验 `payloads/` 离线载荷完整性；
2. 安装 AgentBoot 程序本体到 `~/.agentboot/app`（Windows：`%LOCALAPPDATA%\AgentBoot\app`）；
3. Python：Linux/macOS 使用系统自带 python3（绝大多数发行版自带）；Windows 若无 Python，
   自动从离线包部署**便携版 Python**（`payloads/python/win-embed.zip`，免管理员权限）；
4. Node：系统没有 Node 时，自动从离线包部署便携 Node 22 运行时（`payloads/node/<平台>/`）；
5. 将所选 Agent 的离线载荷直接落盘到 `~/.agentboot/agents/<id>/node_modules`（**不执行 npm**），
   并生成 `claude` / `codex` / `qwen` 等命令的启动 shim；
6. 把 `~/.local/bin`（Windows：`%LOCALAPPDATA%\AgentBoot\bin`）注册进用户 PATH（幂等）；
7. 内置 Agent `ab` 随装随用：联网机器默认 Agnes 免费模型；纯离线机器在菜单[4]配置本地模型。

### 离线包内容结构

```
AgentBoot/
├── install-offline.sh / install-offline.ps1   ← 离线一键安装脚本
├── MANIFEST.txt                                ← 包内清单（版本/平台/Agent 列表）
├── core/ agents/ tools/ scripts/ ...           ← 程序本体
└── payloads/
    ├── python/win-embed.zip                    ← Windows 便携 Python
    ├── node/<linux-x64|win-x64|darwin-arm64>/  ← 各平台便携 Node 22
    └── agents/<agent-id>/<平台>/node_modules/  ← 各 Agent 的完整离线依赖
```

### 离线安装常见问题

| 现象 | 处理 |
|---|---|
| `未找到 payloads/ 离线载荷目录` | 脚本必须位于完整解压后的包根目录；或用 `--payload <路径>` 指定 |
| Linux 上没有 python3 | `apt install python3` / `dnf install python3` / `apk add python3` 后重跑 |
| 装完命令找不到 | 重开终端；或手动 `export PATH="$HOME/.local/bin:$PATH"` |
| 某 Agent 提示平台载荷缺失 | 该离线包构建时未包含此平台（用方式 B 自建，加 `-Platforms` 参数） |
| 纯离线机器上 `ab` 连不上模型 | 正常——Agnes 需联网。菜单[4]配置本地模型（Ollama/LM Studio）后完全离线可用 |

---

## 🌏 中国网络环境自适应

AgentBoot 对国内复杂网络做了开箱即用的处理：

- **自动判定**：安装/运行时探测 `registry.npmjs.org` 连通性，不通则自动判定为中国大陆网络；
- **npm 镜像**：自动切换到 `https://registry.npmmirror.com` 安装所有 npm 类 Agent；
- **Node 运行时镜像**：便携 Node 从 npmmirror 二进制镜像下载（备用 nodejs.org）；
- **pip 镜像**：Aider 等 Python 工具自动使用清华 PyPI 镜像；
- **多源下载容错**：安装包下载按 `boot.ide.pub（Cloudflare） → GitHub → ghfast.top → gh-proxy.com` 顺序自动重试；
- **代理支持**：菜单 `[5] 镜像与代理设置` 可一键为 npm 与 AgentBoot 自身配置 HTTP 代理；
- **手动强制**：环境变量 `AGENTBOOT_MIRROR=cn|off` 可强制开启/关闭镜像模式（默认自动）。

## 🧠 模型配置

菜单 `[4] 模型配置` 支持：

1. **Agnes 免费模型（官方预设，默认）**——已内置接入信息，无需填写：

   | 项 | 值 |
   |---|---|
   | Base URL | `https://apihub.agnes-ai.com/v1` |
   | 模型 ID | `agnes-2.5-flash` |
   | 说明 | 官方预设的**永久免费**模型 |

2. **自定义 OpenAI 兼容接口**：任意中转/私有部署（输入 Base URL、Key、模型 ID）；
3. **本地模型（完全离线）**：Ollama（`http://127.0.0.1:11434/v1`）、LM Studio（`http://127.0.0.1:1234/v1`）、vLLM 等；
4. **连通性测试**：一键验证当前模型可用性。

配置保存在 `~/.agentboot/config.json`，也可直接编辑。

## 🧰 内置最小 Agent（ab）能力

- 单文件、零第三方依赖（仅 Python 标准库），启动即用；
- 工具：执行命令（危险命令拦截/确认）、读写与精确编辑文件、目录列表、抓取网页；
- **离线 Linux 知识库**：内置 9 大主题（基础命令/系统信息/服务管理/网络/软件包/用户权限/磁盘/故障排查/终端技巧），
  问它 Linux 用法、让它操作 Linux、修简单问题（磁盘满、端口占用、服务起不来等）都可以；
- 模型随时切换（`/model`），支持流式输出。

示例：

```bash
ab                                  # 进入交互
你 › 查一下磁盘占用，满的话给出清理建议
你 › nginx 起不来怎么排查？
你 › /linux 查看端口占用
```

## 🔧 升级与卸载

```bash
# 升级：重跑在线一键安装即可覆盖更新（配置保留）
curl -fsSL https://boot.ide.pub/install.sh | sh

# 卸载
rm -rf ~/.agentboot ~/.local/bin/agentboot ~/.local/bin/ab      # Linux/macOS
rmdir /s /q "%USERPROFILE%\.agentboot" & rmdir /s /q "%LOCALAPPDATA%\AgentBoot"   # Windows
```

## ❓ 在线安装故障排查

| 现象 | 处理 |
|---|---|
| `curl` 不存在 | 用 `wget -qO- https://boot.ide.pub/install.sh \| sh`，或先安装 curl |
| 所有下载源失败 | 用离线包安装；或给终端设置代理后重试 |
| Windows 提示"禁止运行脚本" | 使用文档中带 `-ExecutionPolicy Bypass` 的命令 |
| `python3` 自动安装失败 | 手动安装后重跑脚本；或先只用其他 Agent（Node 类不依赖 Python） |
| 装完 `agentboot` 找不到命令 | 重开终端；或 `source ~/.bashrc`；Windows 重开终端窗口 |
